import React from 'react';

export default function RepositoryItem({
   name,
   url,
   descriptionHTML,
   primaryLanguage,
   owner,
   stargazers,
   watchers,
   viewerSubscription,
   viewerHasStarred,
                                       }){

    return(<View />)

}